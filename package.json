{
  "name": "openclaw-qmd",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw plugin for integrating the local qmd CLI.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.48"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "openclaw": "^2026.3.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "openclaw": "^2026.3.2"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
